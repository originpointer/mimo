# Manus 浏览器插件逆向实现分析报告

## 1. 项目概述 (Project Overview)

| 属性 | 值 |
|------|-----|
| **项目名称** | Manus AI Browser Operator 逆向工程分析 |
| **目标版本** | 0.0.47 |
| **构建日期** | 2026-01-21 21:07:38 |
| **扩展类型** | Chrome Manifest V3 扩展 |
| **总大小** | 888KB |
| **分析日期** | 2026-01-28 |

**扩展描述**: Manus AI Browser Operator 是一个 Chrome 浏览器扩展，通过 AI Agent 集成实现浏览器自动化。扩展注入到所有网页，能够模拟用户交互（点击、输入、滚动）、捕获截图、提取页面内容，并通过 WebSocket 与 Manus AI 后端通信。

---

## 2. 逆向工程方法论 (Reverse Engineering Methodology)

### 2.1 工具选择与安装

项目共测试了 8 个 JavaScript 逆向工程工具，成功安装 5 个（62.5% 成功率）：

| 工具 | 类型 | 状态 | 用途 |
|------|------|------|------|
| **javascript-deobfuscator** | CLI/库 | ✅ 成功 | Shift-AST 基于的通用 JS 反混淆 |
| **synchrony** | CLI/库 | ✅ 成功 | javascript-obfuscator.io 专用反混淆 |
| **SecretFinder** | CLI | ✅ 成功 | API 密钥和凭据发现 |
| **LinkFinder** | CLI | ✅ 成功 | API 端点提取 |
| **ai-code-decompile** | Web 界面 | ⚠️ 部分 | AI 驱动的现代框架分析 |
| **jsluice** | CLI | ❌ 失败 | 需要 Go 环境 |
| **deobfuscate-js** | Web 界面 | ❌ 失败 | Ruby/Jekyll 栈问题 |
| **de4js** | Web 界面 | ❌ 重复 | 与 deobfuscate-js 重复 |

### 2.2 分析阶段

```
┌─────────────────────────────────────────────────────────────────┐
│                      阶段1: 自动化分析                          │
│  - js-beautify 代码美化                                        │
│  - LinkFinder 端点提取                                         │
│  - SecretFinder 密钥扫描                                       │
│  - 正则表达式模式匹配                                          │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      阶段2: 手动代码审查                        │
│  - 消息流重建                                                  │
│  - 认证机制追踪                                                │
│  - 架构组件识别                                                │
│  - 安全权限审计                                                │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      阶段3: 文档化                              │
│  - 结构化文档创建 (15+ 文档)                                   │
│  - ASCII 架构图绘制                                            │
│  - 消息类型分类 (50+ 类型)                                     │
│  - 伪代码重构                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 扩展架构分析 (Extension Architecture Analysis)

### 3.1 整体架构

> **重要说明 (2026-01-28 更新)**: 原始分析报告中关于 "sidepanel" 的描述有误。经验证，扩展**没有使用 Chrome 原生 sidePanel API**。实际的 UI 界面位于 `manus.im` 网站上，通过 `ManusAppBridge` 与扩展通信。详见 [analysis/09_重要发现/sidepanel实现方式发现报告.md](analysis/09_重要发现/sidepanel实现方式发现报告.md)。

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Manus AI 扩展架构                            │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────┐         ┌──────────────────┐
    │   https://manus.im   │         │   任意网页        │
    │                      │         │                  │
    │  ┌────────────────┐  │         │  ┌────────────┐  │
    │  │   React UI     │  │         │  │ Content     │  │
    │  │   (任务控制)    │  │         │  │ Script      │  │
    │  └────────┬───────┘  │         │  │ (DOM交互)   │  │
    └───────────┼──────────┘         └───┼────────────┘  │
                │                      │                 │
                │ window.postMessage   │ chrome.tabs     │
                │ (ManusAppBridge)     │ sendMessage     │
                │                      │                 │
                ▼                      ▼                 ▼
    ┌──────────────────────────────────────────────────────────────┐
    │                  Background Worker                           │
    │  ┌───────────────┐  ┌───────────────┐  ┌─────────────┐     │
    │  │ManusAppHandler│  │ContentHandler │  │SessionManager│    │
    │  │(处理网站消息)  │  │(处理页面消息)  │  │ (会话管理)   │     │
    │  └───────────────┘  └───────────────┘  └─────────────┘     │
    │  ┌───────────────┐  ┌───────────────┐  ┌─────────────┐     │
    │  │  AuthHelper   │  │  CdpClient    │  │  TabManager  │     │
    │  │  (Cookie同步) │  │  (截图/调试)  │  │ (标签管理)   │     │
    │  └───────────────┘  └───────────────┘  └─────────────┘     │
    └───────────────────────────────┬──────────────────────────────┘
                                    │
                     chrome.debugger.sendCommand
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │ Chrome DevTools Protocol      │
                    │  - Page.captureScreenshot     │
                    │  - Runtime.evaluate           │
                    │  - DOM manipulation           │
                    └───────────────────────────────┘
```

### 3.2 核心组件分析

#### 3.2.1 后台工作器 (background.ts.js)

| 属性 | 值 |
|------|-----|
| 文件大小 | 163KB |
| 美化后行数 | 6813 行 |
| 类型 | TypeScript 编译为 JavaScript (ES6 模块) |

**主要类架构**:
```
background.ts.js
├── AuthHelper (mn)              - Cookie 认证同步
├── CdpClient (函数集合)         - Chrome DevTools Protocol
├── ContentHandler (Bn)          - Content Script 消息
├── ManusAppHandler (Fn)         - Manus 应用消息
├── PopupHandler (Re)            - Popup UI 消息
├── SessionManager (Vn)          - 任务会话生命周期
├── TabManager (Yn)              - 标签页组管理
├── WebSocketClient (ms)         - WebSocket 连接
├── ScenarioRunner (bs)          - 自动化场景执行
├── ElementHelper (U)            - DOM 元素辅助
└── ActionExecutor (Es)          - 浏览器操作执行
```

**关键功能**:

| 组件 | 行号 | 职责 |
|------|------|------|
| AuthHelper | 59-195 | 监听 manus.im Cookie 变化，同步到 chrome.storage.local |
| CdpClient | 197-600+ | CDP 会话管理、截图、重试机制 |
| SessionManager | 1034-1116 | 会话生命周期、状态管理 |
| TabManager | 1117-1460 | 标签页组、emoji 动画、会话标记 |

#### 3.2.2 内容脚本 (content.ts.js)

| 属性 | 值 |
|------|-----|
| 文件大小 | 120KB |
| 美化后行数 | 5000+ 行 |
| 注入时机 | document_start (DOM 构建前) |

**注入策略**:
```javascript
// manifest.json
{
  "content_scripts": [{
    "matches": ["<all_urls>"],
    "js": ["assets/content.ts-loader.js"],
    "run_at": "document_start",
    "all_frames": false
  }]
}
```

**核心功能**:
- DOM 元素分析和可见性检查
- 交互元素检测和标记
- 用户交互模拟（点击、输入、滚动）
- 蓝色视觉反馈系统
- 状态栏显示

#### 3.2.3 核心插件模块

| 文件 | 大小 | 描述 |
|------|------|------|
| `sendMessage.js` | 2.7KB | 消息传递基础设施、环境配置、日志系统 |
| `token.js` | 3.4KB | 存储抽象层、认证令牌管理、浏览器设置 |
| `chromeAsync.js` | 2.7KB | Chrome API Promise 封装 |
| `manus.js` | 293B | Manus 域名验证 |
| `typeGuards.js` | 266B | TypeScript 类型守卫 |

---

## 4. 关键技术实现 (Key Technical Implementations)

### 4.1 认证机制

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户登录流程                                  │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│              1. 用户在 manus.im 登录                                │
│              2. 后端设置 session_id Cookie                          │
│              - HttpOnly: true (JS 无法访问)                         │
│              - Secure: true (仅 HTTPS)                              │
│              - SameSite: Lax/Strict                                 │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│              3. chrome.cookies.onChanged 事件                       │
│              4. AuthHelper 检测到 Cookie 变更                       │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│              5. Cookie 值同步到 chrome.storage.local                │
│              - 键: manus_extension_token                           │
│              - 值: session_id 的值                                  │
│              - 防抖: 500ms                                          │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│              6. 所有扩展上下文通过 liveUpdate 获取令牌              │
└─────────────────────────────────────────────────────────────────────┘
```

**Cookie 映射**:

| Cookie 名称 | 用途 | 存储键 |
|------------|------|--------|
| `session_id` | 认证令牌 | `manus_extension_token` |
| `devBranch` | 开发分支标志 | `manus_extension_dev_branch` |

### 4.2 消息系统

**50+ 消息类型分类**:

| 类别 | 数量 | 示例 |
|------|------|------|
| 会话管理 | 8 | `session/start`, `session/stop`, `session/status` |
| 自动化操作 | 12 | `automation/click`, `automation/type`, `automation/scroll` |
| 页面交互 | 6 | `page/check-ready`, `page/event-block`, `page/ready-state` |
| 用户干预 | 3 | `extension/stop-task`, `extension/unauthorize-task` |
| Manus 应用 | 3 | `my-browser/ping`, `my-browser/switch-to-tab` |

**消息流示例**:
```
Manus.im (Web UI)           Background                Content/CDP
    │                            │                         │
    │ my-browser/start-session   │                         │
    ├───────────────────────────>│                         │
    │                            │ createTab()             │
    │                            │ groupTabs()            │
    │                            │ attachDebugger()       │
    │                            ├────────────────────────>│
    │                            │ Page.enable            │
    │                            │<────────────────────────┤
    │                            │                         │
    │ extension/session/started  │                         │
    │<───────────────────────────┤                         │
    │                            │                         │
    │ automation/run-scenario    │                         │
    ├───────────────────────────>│                         │
    │                            │ page/check-ready       │
    │                            ├────────────────────────>│
    │                            │<────────────────────────┤
    │                            │ page/ready-state       │
    │                            │ automation/click       │
    │                            ├────────────────────────>│
    │                            │<────────────────────────┤
    │ automation/progress        │                         │
    │<───────────────────────────┤                         │
```

### 4.3 Chrome DevTools Protocol (CDP) 集成

**配置**:
```javascript
const CDP_VERSION = "1.3"
const SESSION_TIMEOUT = 60000  // 60 秒不活跃超时
const MAX_RETRIES = 2          // 最多重试 2 次
```

**CDP 功能**:

| 命令 | 用途 |
|------|------|
| `Page.enable` | 启用页面域 |
| `Page.captureScreenshot` | 捕获截图 |
| `Page.getLayoutMetrics` | 获取布局指标 |
| `Runtime.evaluate` | 执行 JavaScript 代码 |

**会话管理**:
- CDP 会话缓存 60 秒
- 不活跃时自动分离调试器
- 失败后自动重试（最多 3 次）

### 4.4 元素定位策略

| 策略 | 描述 | 示例 |
|------|------|------|
| `bySelector` | CSS/XPath 选择器 | `{ strategy: "bySelector", selector: "button.submit" }` |
| `byIndex` | 预分配的数字索引 | `{ strategy: "byIndex", index: 42 }` |
| `byCoordinates` | 视口坐标 | `{ strategy: "byCoordinates", x: 100, y: 200 }` |

### 4.5 会话状态管理

```
pending → running → stopped
              ↓
         takeover (用户接管)
              ↓
         running (恢复)
```

**状态定义**:

| 状态 | 描述 | UI 效果 |
|------|------|---------|
| `stopped` | 会话未启动或已停止 | 无动画 |
| `running` | 任务正在执行 | 动态 emoji (👆🖐️👋👍...) |
| `takeover` | 用户已接管控制 | "停止任务" 按钮 |
| `error` | 发生错误 | 错误提示 |

---

## 5. 安全分析 (Security Analysis)

### 5.1 权限审计

**Manifest 权限**:
```json
{
  "permissions": [
    "tabs",          // 标签页管理
    "tabGroups",     // 标签页分组
    "storage",       // 本地存储
    "debugger",      // Chrome DevTools Protocol
    "cookies",       // Cookie 访问
    "scripting"      // 动态脚本注入
  ],
  "host_permissions": ["<all_urls>"]
}
```

**权限风险评估**:

| 权限 | 风险级别 | 用途 | 潜在风险 |
|------|----------|------|---------|
| `debugger` | **高** | 截图、DOM 操作、页面控制 | 完全控制页面内容和行为 |
| `scripting` | **高** | 注入 Content Script | 可在任意页面执行代码 |
| `<all_urls>` | **高** | 所有网站访问 | 无限制访问所有网页 |
| `cookies` | **中** | 读取 manus.im Cookie | 可读取任意域的 Cookie |
| `tabs` | **中** | 读取标签页信息 | 可访问所有标签页 URL 和标题 |
| `storage` | **低** | 存储设置和令牌 | 本地数据访问 |
| `tabGroups` | **低** | 管理标签页组 | 仅影响标签页组织 |

### 5.2 数据收集分析

**收集的数据**:
- ✅ 页面 URL 和标题
- ✅ 页面截图（Base64 编码）
- ✅ 页面文本内容（Markdown 格式）
- ✅ 交互元素（按钮、链接等）
- ✅ 用户交互（点击、输入）

**不收集的数据**:
- ❌ 密码字段
- ❌ 完整 Cookie（仅同步 session_id）
- ❌ 浏览器历史
- ❌ 下载文件

### 5.3 传输安全

| 端点类型 | 地址 | 加密 |
|---------|------|------|
| WebSocket (生产) | `wss://api.manus.im` | TLS (wss://) |
| WebSocket (开发) | `wss://vida.butterfly-effect.dev` | TLS (wss://) |
| WebSocket (本地) | `ws://localhost:4000` | 无 (本地仅) |
| HTTP API | `https://manus.im/*` | TLS (HTTPS) |

**数据格式**:
- 截图: Base64 编码图片
- 页面内容: Markdown 格式
- 事件: JSON 格式
- 状态: JSON 格式

### 5.4 令牌存储安全

| 属性 | 值 |
|------|-----|
| 存储位置 | `chrome.storage.local` |
| 键名 | `manus_extension_token` |
| 类型 | 字符串 |
| 加密 | ❌ 无（明文） |
| Content Script 访问 | ❌ 无直接访问 |

**安全风险**: 令牌以明文形式存储在 chrome.storage.local 中。

### 5.5 域名验证

**ManusAppBridge 验证**:
```javascript
function isManusAppOrigin(origin) {
  const { webAppDomain } = Environment.getEnvParams()

  // 生产环境
  if (origin === "https://manus.im") return true

  // 开发模式：允许本地主机
  if (Environment.isDev()) {
    const url = new URL(origin)
    const isLocalhost =
      url.hostname === "localhost" ||
      url.hostname === "127.0.0.1" ||
      url.hostname === "::1"

    if (url.protocol === "http:" && isLocalhost) {
      return true
    }
  }

  return false
}
```

---

## 6. 文件结构映射 (File Structure Mapping)

### 6.1 源代码文件

> **注意**: `sidepanel.html`, `sidepanel.html.js`, `sidepanel.css` 等文件存在但**未被使用**。实际 UI 位于 `manus.im` 网站上。

```
sources/0.0.47_0/
├── manifest.json              # 扩展配置清单
├── background.ts.js           # Service Worker (163KB, 6813行)
├── content.ts.js              # Content Script (120KB, 5000+行)
├── sidepanel.html.js          # ❌ 未使用 (早期遗留文件)
├── popup.html.js              # React 弹窗 (9.3KB)
├── eye.js                     # UI 组件库 (141KB)
├── helper.js                  # 工具函数 (78KB)
├── sendMessage.js             # 消息传递 (2.7KB)
├── token.js                   # 存储管理 (3.4KB)
├── chromeAsync.js             # Chrome API 封装 (2.7KB)
├── manus.js                   # 域名验证 (293B)
├── typeGuards.js              # 类型守卫 (266B)
├── ManusAppBridge.ts.js       # Web App 桥接 (注入到 manus.im)
├── service-worker-loader.js   # Service Worker 加载器
├── assets/
│   ├── content.ts-loader.js   # Content Script 加载器
│   ├── ManusAppBridge.ts-loader.js
│   ├── popup.css              # 弹窗样式
│   └── sidepanel.css          # ❌ 未使用
├── src/
│   ├── popup.html             # 弹窗 HTML
│   └── sidepanel.html         # ❌ 未使用
└── icons/                     # 各种尺寸图标
    ├── 16.png, 32.png, 48.png, 128.png
    └── 16-dev.png, 32-dev.png, 48-dev.png, 128-dev.png
```

### 6.2 分析文档结构

```
manus-reverse/
├── analysis/                  # 核心分析文档 (9 个类别)
│   ├── 00_概述/               # 概述和总结
│   │   ├── 总结报告.md        # 370 行综合报告
│   │   └── 消息类型分类.md    # 50+ 消息类型
│   ├── 01_核心插件/           # 核心插件分析 (5 个文件)
│   │   ├── 消息传递机制.md    # sendMessage.js
│   │   ├── 令牌管理.md        # token.js
│   │   ├── Chrome异步封装.md  # chromeAsync.js
│   │   ├── Manus集成.md       # manus.js
│   │   └── 类型守卫.md        # typeGuards.js
│   ├── 02_后台工作器/         # Background Worker (2 个文件)
│   │   ├── 架构分析.md        # 495 行架构文档
│   │   └── 认证助手.md        # 438 行 AuthHelper 分析
│   ├── 03_内容脚本/           # Content Script
│   │   └── 注入策略.md        # 560 行注入策略
│   ├── 04_UI组件/             # UI 组件
│   ├── 05_安全分析/           # 安全分析
│   │   └── 权限审计.md        # 357 行权限审计
│   ├── 06_自动化能力/         # 自动化能力
│   ├── 07_提取工具/           # 工具提取结果
│   │   ├── endpoints-background.html
│   │   ├── endpoints-content.html
│   │   └── 密钥提取结果.html
│   ├── 08_代码重构/           # 代码重构
│   └── 09_重要发现/           # 重要发现 (2026-01-28 新增)
│       └── sidepanel实现方式发现报告.md
├── guides/                    # 工具使用指南 (8 个指南)
├── projects/                  # 安装的逆向工程工具
├── sources/                   # 原始扩展源代码
├── SUMMARY.md                 # 主总结文档
└── clean.sh                   # 清理脚本
```

---

## 7. 分析产物清单 (Analysis Deliverables)

### 7.1 文档列表 (15+ 文档, 5000+ 行)

| 文档 | 行数 | 描述 |
|------|------|------|
| **总结报告.md** | 370 | 综合逆向工程报告 |
| **消息类型分类.md** | 286 | 50+ 消息类型分类和流程 |
| **消息传递机制.md** | 245 | sendMessage.js 分析 |
| **令牌管理.md** | 317 | token.js 存储管理 |
| **Chrome异步封装.md** | - | chromeAsync.js 分析 |
| **Manus集成.md** | - | manus.js 域名验证 |
| **类型守卫.md** | - | typeGuards.js 类型安全 |
| **架构分析.md** | 495 | background.ts.js 架构 |
| **认证助手.md** | 438 | AuthHelper 详细分析 |
| **注入策略.md** | 560 | content.ts.js 注入策略 |
| **权限审计.md** | 357 | 安全审计和权限分析 |

### 7.2 工具输出

| 文件 | 描述 |
|------|------|
| `endpoints-background.html` | LinkFinder 提取的后台端点 |
| `endpoints-content.html` | LinkFinder 提取的内容脚本端点 |
| `endpoints-sendMessage.html` | LinkFinder 提取的消息端点 |
| `密钥提取结果.html` | SecretFinder 发现的凭据 |

### 7.3 ASCII 架构图

- 整体架构图
- 消息流程图
- 认证流程图
- 数据流图
- 会话状态转换图

---

## 8. 关键发现 (Key Findings)

### 8.1 技术发现

1. **消息驱动架构**: 所有组件通过消息通信，实现松耦合的模块化设计

2. **CDP 持久化会话**: CDP 会话缓存 60 秒，避免频繁附加/分离调试器，提高性能

3. **Cookie 认证同步**: 500ms 防抖机制确保 Cookie 变更同步效率

4. **三种元素定位策略**: 支持选择器、索引、坐标三种定位方式，提高自动化灵活性

5. **Emoji 动画视觉反馈**: 任务进行时显示动态 emoji (👆🖐️👋👍...)，完成后显示 ✅

6. **document_start 注入**: Content Script 在 DOM 构建前注入，确保最早捕获页面事件

7. **MobX 状态管理**: 使用 MobX 实现响应式状态，跨上下文实时同步

### 8.2 安全发现

1. **令牌明文存储**: 认证令牌以明文形式存储在 chrome.storage.local

2. **debugger 权限强大**: 提供完整的页面控制能力，包括 DOM 操作和截图

3. **域名验证保护**: PostMessage 通信严格验证域名，防止跨站攻击

4. **Content Script 隔离**: Content Script 无法直接访问令牌，降低泄露风险

5. **类型守卫保护**: 使用 TypeScript 类型守卫验证消息来源

---

## 9. 适用场景与建议 (Use Cases & Recommendations)

### 9.1 适用场景

| 场景 | 描述 |
|------|------|
| ✅ 重复性网页操作自动化 | 表单填写、数据录入等重复任务 |
| ✅ 数据抓取和分析 | 网页数据提取和分析 |
| ✅ 浏览器测试 | 自动化测试和验证 |
| ✅ 个人工作流自动化 | 日常任务自动化处理 |

### 9.2 不适用场景

| 场景 | 原因 |
|------|------|
| ❌ 敏感环境 (银行、医疗) | 数据收集风险较高 |
| ❌ 完全离线场景 | 需要连接 Manus API |
| ❌ 高隐私要求场景 | 收集页面内容和截图 |

### 9.3 改进建议

| 建议 | 优先级 | 描述 |
|------|--------|------|
| 令牌加密存储 | 高 | 使用 chrome.crypto API 加密存储令牌 |
| 权限最小化 | 中 | 评估是否可以减少某些权限的使用范围 |
| 审计日志 | 中 | 记录所有敏感操作的审计日志 |
| 隐私政策 | 高 | 提供清晰的隐私政策说明数据收集和使用 |
| 用户控制 | 中 | 提供更细粒度的用户控制选项 |

---

## 10. 总结 (Conclusion)

### 10.1 技术总结

Manus AI Browser Extension 是一个**功能完善的浏览器自动化工具**，通过以下核心技术实现 AI 驱动的任务自动化：

1. **消息驱动架构**: 所有组件通过消息通信，实现模块化设计
2. **CDP 集成**: 使用 Chrome DevTools Protocol 进行高级浏览器控制
3. **Cookie 认证**: 与 Manus Web App 共享认证状态
4. **响应式状态**: MobX 实现跨上下文状态同步
5. **用户控制**: 提供随时可停的视觉反馈

### 10.2 安全总结

**整体安全评级**: ⚠️ **中等风险**

**主要风险**:
- debugger 权限过于强大
- 令牌明文存储
- 广泛的主机权限 (<all_urls>)

**缓解措施**:
- 用户可见的视觉反馈
- 随时可停止的任务
- 域名验证和类型守卫
- 详细的日志记录

### 10.3 逆向工程总结

本项目采用**系统化的逆向工程方法**：

1. **工具选择**: 测试 8 个工具，成功安装 5 个 (62.5%)
2. **多阶段分析**: 自动化分析 → 手动审查 → 文档化
3. **全面文档化**: 15+ 分析文档，5000+ 行内容
4. **深度技术分析**: 覆盖架构、消息流、认证、安全等各方面

本分析报告为理解 Manus AI Browser Extension 的技术实现提供了全面的参考，也为浏览器扩展的逆向工程分析提供了系统化的方法论示例。

---

## 附录

### A. 技术栈

| 技术 | 用途 |
|------|------|
| Vite | 构建工具 |
| TypeScript | 源语言 |
| React 19 | UI 框架 |
| MobX | 状态管理 |
| CSS Variables + Tailwind CSS | 样式 |
| Lucide React | 图标 |
| Chrome Extension API V3 | 浏览器 API |
| Chrome DevTools Protocol 1.3 | 调试协议 |

### B. 网络端点

| 端点 | 用途 |
|------|------|
| `wss://api.manus.im` | 生产 WebSocket |
| `wss://vida.butterfly-effect.dev` | 开发 WebSocket |
| `ws://localhost:4000` | 本地 WebSocket |
| `https://manus.im/my-browser` | 扩展设置 |
| `https://manus.im/login` | 登录页面 |
| `https://manus.im/app` | 主应用 |

### C. 会话状态定义

```javascript
{
  sessionId: string,           // 会话 ID
  tabId: number,               // 主标签页 ID
  groupId?: number,            // 标签页组 ID
  windowId: number,            // 窗口 ID
  status: "stopped" | "running" | "takeover" | "error",
  taskName?: string,           // 任务名称
  animationInterval?: number,  // emoji 动画定时器
  queue: Promise<>,            // 操作队列
  disposed: boolean            // 是否已释放
}
```

### D. 浏览器设置结构

```javascript
{
  browserName: string,         // 随机生成 (如 "Swift-Fox")
  allowCrossBrowser: boolean,  // 是否允许跨浏览器操作
  skipAuthorization: boolean   // 是否跳过授权
}
```

---

**报告生成时间**: 2026-01-28
**分析工具**: javascript-deobfuscator, synchrony, SecretFinder, LinkFinder, js-beautify
**文档版本**: 1.0
