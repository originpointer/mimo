截屏功能        参数为tabId           返回值为图像数据      不需要active
语义话元素选择  参数为json             返回值为 xpath        不需要active
页面可交互元素扫描 参数为dom selector   返回值为 xpath          不需要active
页面内容获取    参数为xpath                返回值为字符串        不需要active
元素标记        参数为xpath           返回值为被标记元素数量  不需要active

综合现有能力的结合方式文档

三个端的数据打通，先结合大模型验证效果

流程编排
