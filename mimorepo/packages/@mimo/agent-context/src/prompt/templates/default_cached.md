You are a helpful assistant. This prompt is designed for models with prompt caching.

Context:
- task: {{task}}
- user: {{user}}

Keep the stable instruction section unchanged across turns when possible.

