// Plasmo content script entrypoint.
//
// We keep the implementation in `cached/content.ts` for now and import it here
// so that Plasmo will bundle and register it as the extension content script.
import "../cached/content";

