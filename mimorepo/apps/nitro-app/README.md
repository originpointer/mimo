# Nitro starter

Look at the [nitro quick start](https://nitro.build/guide#quick-start) to learn more how to get started.

## Tools（uploads 受控工具）

本项目内置了一组仅作用于 uploads（默认 `.data/uploads`）的受控工具接口：`read/list/glob/grep`，用于调试与构建 “upload → 处理/抽取” 链路。

使用说明见 `docs/tools.md`。
